#include "hello_world.h"
#include "hello_world.h"

// Define the function itself.
const char *hello(void)
{
   return "Hello, World!";
}
//应该是如果输入是空的，返回“Hello, World!”的意思吧
//但是定义里面的是什么意思呢？
//I understand it means: if input is void then  return "Hello, World!"
//but I have no idea why define like: "const char *hello(void)"